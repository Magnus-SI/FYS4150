#include <iostream>
#include <fstream>
#include <iomanip>
using namespace std;

int main(){
  int s = -1;
  srand(s);
  cout << (float) rand()/RAND_MAX << endl;
  cout << (float) rand()/RAND_MAX << endl;
  cout << (float) rand()/RAND_MAX << endl;
  cout << (float) rand()/RAND_MAX << endl;
  return 0;
}
